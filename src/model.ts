export interface Chats {
    author?: string;
    ids?: string[];
    messages?: string;
    timestamp?: Date;
    id?: string;
}

export interface User {
    avatar?: string;
    timestamp?: Date;
    user?: string;
    id?: string;
}
