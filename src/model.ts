export interface Chats {
    author?: string;
    ids?: string[];
    message?: string;
    timestamp?: any;
    id?: string;
}

export interface User {
    avatar?: string;
    timestamp?: Date;
    user?: string;
    id?: string;
}
